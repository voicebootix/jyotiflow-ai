import React from 'react';

const AdminRedirect = () => {
  // COMPLETELY REMOVED: All admin redirect logic
  // Admin users can now navigate like normal users
  // No auto-redirects, no banners, no special behavior
  
  return null;
};

export default AdminRedirect; 