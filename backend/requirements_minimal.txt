# Minimal requirements for knowledge seeding worker only
# Core web framework (needed for imports)
fastapi==0.111.0
uvicorn[standard]==0.24.0

# Database - asyncpg for async PostgreSQL operations
asyncpg==0.29.0
pgvector==0.2.5

# HTTP requests for RSS feeds
aiohttp==3.9.5
requests==2.32.3

# RSS feed parsing
feedparser==6.0.11

# OpenAI for embeddings
openai==1.35.3

# Basic utilities that work with Python 3.13
python-dotenv==1.0.1
python-dateutil==2.9.0
structlog==24.2.0

# Authentication (if needed for imports)
PyJWT==2.8.0
bcrypt==4.1.2

# Async HTTP dependencies
yarl==1.9.4
multidict==6.6.4
aiosignal==1.4.0
frozenlist==1.7.0
async-timeout==5.0.1
attrs==25.3.0

# Other core dependencies
certifi==2025.8.3
charset-normalizer==3.4.3
idna==3.10
urllib3==2.5.0
